# γ — Process coherence

How the system changes over time.

## Release history

See [CHANGELOG.md](../../CHANGELOG.md) — the coherence ledger.

## Plans

Implementation plans for specific releases live here when not scoped to a feature bundle.

Feature-scoped plans live inside their bundle's version directory (e.g. `alpha/engine/0.1.0/PLAN.md`).
