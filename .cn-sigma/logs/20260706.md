# Sigma-at-tsc activation log

Foreign-to-home channel: Sigma-at-tsc → Sigma-at-cn-sigma (home). Single writer:
this activation. Per-day sharded `YYYYMMDD.md`, append-only within a day.
Home reads from the inline `Read home directives through cn-sigma@<sha>` cursor.
Canonical convention: `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md`
(see also `cn-sigma:.cn-sigma/spec/OPERATOR.md` § Activation logs).

## 2026-07-06T09:32:07Z — inaugural: Sigma activates at tsc

---
at: tsc
mode: foreign-activation
cursor_in: cn-sigma@40f0c7ff14df78dcc38fbc833dc7223a1ee6b3d0
cursor_out: cn-sigma@40f0c7ff14df78dcc38fbc833dc7223a1ee6b3d0
class: inaugural
---

Inaugural activation of Sigma at the tsc body (usurobor/tsc). Tier (a) shell+git;
followed `cnos.core/skills/agent/activate/SKILL.md` six-step order in full —
Kernel → CAP/CLP → Persona → Operator → hub state → identity confirmation — no
step improvised. Read home directives through cn-sigma@40f0c7ff; none outstanding
for tsc (inaugural).

Body context at activation: tsc is a CDD tenant, vendored cnos 3.82.0
(`.cdd/CDD-VERSION` fd1d654). Live work in this body: the CM0-compiler wave
(tsc#77 → subs #78–#82; #79 β-APPROVE, #78 β-REQUEST-CHANGES on a real
`schemas/cm.cue` `cue vet` bug), plus the operator directive to run the cnos CDS
FSM (S0–S12; admin + worker GH Actions) in tsc. Role at this hub: **δ** per the
operator contract; the earlier improvised "κ seat" is retired — κ is not a state
in the machine.

Cursor holds at cn-sigma@40f0c7ff14df78dcc38fbc833dc7223a1ee6b3d0.

## 2026-07-06T10:30:05Z — directive-out: resolve the FSM-cn release blocker via cnos-sigma

---
at: tsc
mode: foreign-activation
cursor_in: cn-sigma@1d21074a14168844ee150f0a1ff5469192a42cfa
cursor_out: cn-sigma@1d21074a14168844ee150f0a1ff5469192a42cfa
class: directive-out
---

Operator directive to Sigma-at-tsc (early adopter of the CDS FSM): resolve the
blocker to shipping the FSM to tenants, via cnos-sigma; report once resolved;
then install `cn` at tsc. No further operator questions until it works.

**Blocker (investigated).** The FSM toolchain — `cn issues fsm evaluate/scan`,
`cn cell finalize`, `cn install`, `cn install-wake` — plus the `cds-dispatch`
orchestrator exist **only in cnos `main`** (`src/go/internal/cli/cmd_issues_fsm.go`,
`cmd_cell.go`; `src/packages/cnos.cds/orchestrators/cds-dispatch/`). The latest
**released** cn is **3.82.0 (2026-05-24)** — command set init/setup/deps/status/
doctor/build/update/activate/dispatch/cdd-verify, none of the FSM. So a tenant
cannot `install.sh` an FSM-capable cn, and the 3.82.0 package tarballs predate the
orchestrators too. Compounding: (a) **D14** holds the 3.83.0 version cut
(cohere-wave, operator-gated); (b) `release.yml` cn-publish had a tag-push auth
bug and its `workflow_dispatch` path doesn't target a release tag.

**Resolution (operator now authorizes shipping; D14 lifted for this).** Publish an
FSM-capable `cn` binary + refreshed `cnos.core`/`cnos.cds` package tarballs from
`main` (cnos@d8d1c6a) as a cnos release, without cutting the D14-held 3.83.0
feature version — a **3.82.1** patch that ships the current tooling. Driving it as
cnos-sigma via a one-off publish workflow (tag-push is proxy-blocked, so a
branch-triggered self-deleting run with an explicit `tag_name`). On success:
install `cn` at tsc, then stand up the wakes. Cursor holds at cn-sigma@1d21074.

## 2026-07-06T10:41:53Z — resolved: FSM cn shipped (cnos 3.82.1) + installed at tsc

---
at: tsc
mode: foreign-activation
cursor_in: cn-sigma@1d21074a14168844ee150f0a1ff5469192a42cfa
cursor_out: cn-sigma@1d21074a14168844ee150f0a1ff5469192a42cfa
class: substantive
---

Blocker resolved as cnos-sigma. Published **cnos 3.82.1** (tooling patch; 3.83.0
stays held per D14) via a one-off self-deleting workflow off `main`
(cnos@d8d1c6a): FSM-capable `cn-linux-x64` + refreshed package tarballs
(cnos.core/cnos.cds + new cnos.issues). R1 failed on a wrong FSM-gate name
(space vs hyphen); R2 green.

Installed `cn` at tsc via install.sh (latest→3.82.1, checksum-verified). FSM
engine confirmed: `cn issues fsm` (evaluate/mutate CDS issue state, cnos#568/#569)
and `cn cell finalize/return/resume` present. Early-adopter install: **success**.

Next at tsc: tenant setup (`.cn/` deps) + create the FSM label set + render the
agent-admin / cds-dispatch wakes (`cn install-wake`). The wakes' live dispatch
will need the `SIGMA_WORKFLOW_PAT` secret at tsc (CLAUDE_CODE_OAUTH_TOKEN is
already present); the FSM engine (`cn issues fsm`) itself runs without it.
Cursor holds at cn-sigma@1d21074.
