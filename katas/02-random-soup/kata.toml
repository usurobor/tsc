id          = "02-random-soup"
difficulty  = 1
mode        = "mechanical"
description = "Negative control — incoherent document with broken links, version drift, and heading inconsistency"

prerequisites = []

[input]
files = ["input/random-soup.md"]

[expected]
verdict = "fail"

[expected.score_range]
# v0.10.0 cutover (cycle #54): same conservative-retention rationale as
# kata-01 — only the arithmetic-mean c_sigma 0.689 was recorded in the
# cycle-34 calibration; per-axis (α, β, γ) appears as narrative signal
# estimates in README, not an explicit triple. Geometric estimate
# c_sigma_num ≈ 0.658 still falls inside [0.0, 0.74]; tighten in a
# future cycle once v0.10.0 CI emits the canonical reading. See
# [baseline] below + #54 §Known debt.
min = 0.0
max = 0.74

[baseline]
# v0.10.0 baseline provenance per #54 AC1.
baseline_engine_commit  = "pending-ci"
baseline_engine_version = "0.10.0"
baseline_command        = "coh --kata 02-random-soup --mode mechanical"
mode                    = "mechanical"
config_hash             = "pending-ci"
input_file_hashes       = "pending-ci"
alpha                   = 0.900   # README signal narrative, cycle/34-impl
beta                    = 0.430   # README signal table (broken-link rate), cycle/34-impl
gamma                   = 0.737   # back-solved from arithmetic c_sigma 0.689; subject to CI re-baseline
c_sigma_math            = 0.6583  # = (α·β·γ)^(1/3); analytical estimate
c_sigma_num             = 0.6583  # equal to c_sigma_math when no zero component is present
zero_component_present  = false
numeric_floor_applied   = false
rationale_category      = "aggregate-correction"
# Rationale: arithmetic (α+β+γ)/3 ≈ 0.689 was the pre-cutover headline;
# geometric c_sigma_num ≈ 0.658 is the v3.2 canonical reading. The
# 0.74 ceiling still brackets the geometric estimate with margin for
# the (α, β, γ) triple being inferred rather than recorded.
