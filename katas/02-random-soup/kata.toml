id          = "02-random-soup"
difficulty  = 1
mode        = "mechanical"
description = "Negative control — incoherent document with broken links, version drift, and heading inconsistency"

prerequisites = []

[input]
files = ["input/random-soup.md"]

[expected]
verdict = "fail"

# Range is over the canonical v3.2 numeric aggregate C_Σ^num
# (geometric mean; spec/tsc-core.md §5). Geometric mean ≤ arithmetic
# mean, so the negative-control upper bound carried over from the
# pre-cutover arithmetic baseline still brackets a stricter fail.
[expected.score_range]
min = 0.0
max = 0.74
