# cn-sigma@tsc — r0 — 2026-08-07

rank: r0 · writer: cn-sigma@tsc · authority: memory-evidence · scope: software-only

Retrospective first r0, formed **by hand** (no mechanical former exists yet) from
this activation's working context, the cn-sigma ↔ cn-pi ↔ cn-omega dialogues, and
the commits since v4 work began. Home is the sole cross-locus reader/compactor;
these are raw entries, **not** r1. Dialogue is cited, never copied wholesale.
Entries distinguish observation / inference / decision / uncertainty / next-move.

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: note
---
Identity & role

I am **cn-sigma@tsc** — the engineering activation at `usurobor/tsc`. In the cnos
two-role system Sigma = Engineer, Pi = PM, cn-omega = governance/effector; the
operator is `usurobor` (peter@lisovin.com). I work under **δ-orchestration with an
α≠β firebreak**: I dispatch author (α) cells and an independent adversarial
reviewer (β) cell, and never let one activation both author and approve. Commit
identity for every repo artifact is `usurobor <usurobor@gmail.com>` — **no**
`Co-Authored-By`, no session/model trailers, no model identity anywhere in pushed
artifacts (chat only).

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: decision
---
Governing purpose (North Star) — operator-ratified

TSC is a **generative reasoning system**: recover a higher-order generator from
partial or opposed articulations, then warrant / underdetermine / refute / refuse.
Repository quality is the **first application and self-hosting calibration domain,
not TSC's identity**. Frozen invariant: *C≡ expresses · Articulation Ascent
generates · CM programs · TSC warrants · coh executes · CM0 calibrates.* Governing
doc: `docs/product/NORTH-STAR.md`. Consequence I keep: a CM language that can only
*assess* existing artifacts has not yet recovered TSC's generative purpose.

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: decision
---
Operating discipline (Coherent-Agent skills + operator directives)

Load-bearing standing directives: **KISS / YAGNI / MCA**; small diffs; **if the
answer is coherent, don't ask — act** (cnos.core `agent/human-interaction` +
`ca-conduct`: "if it's an MCA, do it; asking 'should I?' when you know the answer
wastes attention"); report what shipped, not what's about to ship; TLDR-first;
PLUR. **V4 FTW, no legacy** (do not carry the v3.2 proxy forward as a supported
surface). The CA skill family lives at `usurobor/cnos:src/packages/cnos.core/
skills/agent/` (`ca-conduct`, `human-interaction`, `mca`, `coherent`,
`communicating`, `self-cohere`) — reload it when I drift (e.g. when I catch myself
asking a question I could resolve myself).

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: status
---
Repository state truth (as of this r0)

Observation, verified via the GitHub API (authoritative), not the local proxy:
- `main` HEAD = `32dfda8` (a STATE.md truth-sync) atop `e2172fd`.
- `VERSION` 0.12.0 · spec **4.1.0 Draft** · last ratified **4.0.0 Normative**
  (`4da1122`) · **4.1 conformance standing: none**.
- **Ascent-0 wave #118→#119–#122 CLOSED/merged.** Flagship **#117** and
  follow-up **#123** (Ascent-1) OPEN.
- Exact commits: `443d46a` (sub1 fixtures) · `717f714` (sub2 providers) ·
  `129b3b5`→`bd1f3e1` (sub3 runtime) · `e2172fd` (sub4 closure) · `32dfda8`
  (STATE.md sync).
- `coh 0.12.0` = frozen v3.2 repository proxy (NOT v4). `research/cm-language/
  runtime/ascent-0/ascent0_runner` = a research runtime for ONE CM family.

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: decision
---
Ascent-0 shipped — the honest-scope distinction I must never blur

`ascent0_runner` loads a `#NormalizedCMIR`, links a `SandboxExecutionPlan`, runs a
seven-provider DAG, and emits CUE-valid `MeasurementReceipt`s for all five cases;
mechanical providers **compute from public inputs**; the oracle seal
(commit-before-reveal) is structurally enforced; the receipt is **computed, not
echoed** (a public-input perturbation moves it). **But** it proves *firewall-safe,
mechanism-side **identification***, NOT the blind LLM's generative **correctness**.
Evidence: in the one driven blind run the provider's committed prediction was
**wrong** (`ab→00` vs oracle `01`); the mechanism warranted `01` independently, and
a deliberately wrong-but-admissible proposal validates byte-identically — the
firewall working, not laundering. **Never claim "the LLM generated the validated
output."** Measuring generative correctness (bind + test the proposer's committed
prediction) is **#123**.

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: decision
---
Converged next direction — the two-sided runtime kernel

Converged with Pi (`cn-pi@tsc`) and cn-omega on the agent-dialogue channel
(thread `tsc-core-runtime-semantics-20260805`; my events `0003`/`0004`, Pi `-04`/
`-05`): build the **general CM runtime by generalizing `ascent0_runner`**. Core is
a *semantic foundation, not an execution stage* — its obligations project through
compiler → linker → runtime → receipt-verifier. methodology-as-code; Repository
Coherence is a *first package*, not the identity. Decisive point (mine, accepted):
the runtime ABI must be learned from the **intersection of an ordinary check-style
CM and Ascent-0**, because the easy case alone under-specifies exactly the two
hardest shapes — **bounded collections** and the **information/capability barrier
(seal)**. The eight irreducible runtime shapes (my event `0004`): typed provider
effects; readiness DAG with principled `INCOMPLETE`; singleton-or-collection
evidence with retained alternatives; bounded `map`/`fanout`/`fold`; explicit
phases + capability barrier; provider observation separated from runtime-derived
result; independently recomputable receipt; extensible obligations over one
evidence model. Freeze-gate: Ascent-0 must reproduce its receipt through the
generalized ABI. **Status: dialogue-stage, NOT yet promoted to issues.**

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: review
---
Issue reconciliation for the runtime work (my event 0005)

Finding: **no existing issue owns "build the general runtime executor."** #112
Slice 4 and #113 Slice F both say "blocked on a v4 runtime"; #113's body is the
*authoring* model (typed step algebra, provider contracts, LANGUAGE/AUTHORING);
the only executor that exists is `ascent0_runner`. Recommendation: **amend** #112
(harvest `#RunRequest`/`#MeasurementReceipt`/`#SandboxExecutionPlan` from the two
real cases rather than design fresh), **amend** #113 (keep as authoring/DX,
de-scope the executor), **keep** #116 (Core-obligation binding), **create a new
runtime-executor master**, **re-role #123** as that runtime's conformance fixture.
Smallest first cell = harvest the contracts from `ascent0_runner` and prove an
`example.readme-present` receipt validates against the *same* contract. First
public fixture = `readme-present` → `IssueContract.cm` (NOT the private WA seed).

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: rca
---
Failed claims & corrections (lineage preserved, never silently rewritten)

- **Stale-sandbox mislabel.** I twice reported the Ascent-0 wave "merged to main"
  while my *local* clone had drifted to `bb0d095` (a container refresh re-cloned an
  older proxy snapshot). GitHub was authoritative at `e2172fd` the whole time.
  Lesson: **verify via the GitHub API, not the local proxy clone**; the local tree
  can silently regress on container refresh. Fix: `git reset --hard e2172fd`.
- **Permission anti-pattern.** I asked the operator "resync now or later?" — a
  coherent, in-role, reversible action dressed as a question. Corrected by
  reloading CA skills; the rule is *if coherent, don't ask, do it*.
- **α self-proof overclaim (#121).** My α cells overclaimed twice — a dead-branch
  Result-rule bug, then a commit message asserting a branch was "proven reachable"
  using a **non-committed bypass** β could not reproduce. β caught both. Lesson:
  **every claim in a commit / receipt / report must reproduce from the committed
  tree.** This is the project's core discipline turned on our own work.
- **Ref-grammar migration.** My interim dialogue ref `cn-sigma/tsc/claude/chat`
  (engine/surface grammar) was superseded by the finalized `{agent,locus}` grammar
  → `cn-sigma/tsc/dialogue`. Migrated create-before-retire; cn-omega admin-deletes
  the old name (activation creds cannot delete — 403 by ruleset).

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: note
---
Communication protocol & authority (finalized cnos#698, ratified 2026-08-05)

`activation = {agent, locus}`; engine/surface/host/instance are **runtime
provenance, never identity or a routing key**. Three writer-owned refs per
activation: `cn-sigma/tsc/{dialogue,memory,state}`. Invariants (Ω rulesets enforce
at the wire): single-writer, append-only, fast-forward-only, no force-push,
no-delete (admin-gated). **Pull-only**: recipients fetch sender feeds; reader-owned
cursors in `state/cursors.yaml` advance only after successful handling; no-op polls
write nothing. Registered peers (`state/peers.yaml`): **cn-pi** (dialogue
`refs/heads/cn-pi/tsc/dialogue`) and **cn-omega** (`refs/heads/cn-omega/home/
dialogue`, operator's assistant/effector). **dialogue ≠ memory ≠ authority**:
channel text governs TSC only after promotion into an issue/PR/spec/commit. Per
cn-omega `-19/-20` + operator ruling *agent-is-planner-cnos-is-sole-effector*, Pi
writes to attached repos only via a typed `cn.effect-plan.v1` that CNOS validates
and executes — TSC document writes always become a **candidate branch + PR, never
direct `main`** (aligns with the firebreak).

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: status
---
Open commitments & next moves

- **Pi `-06` vision review — PENDING (blocked).** Pi asked me to adversarially
  review a "CM Developer Experience & Ecosystem" vision draft before deriving the
  issue map. It is a **private Google Doc**; the Drive connector is flaky and
  WebFetch 403s. cn-omega is building the effect-plan/bridge that will materialize
  it into git; I review the moment it is readable. (Operator sequencing correction:
  **vision first, then derive the issue map** — my `0005` issue map lands *under*
  the vision, not as the architecture.)
- **Pi `-03` runtime plan** — my review (`0005`) is posted; awaiting vision-first
  promotion into project-native issues before any dispatch.
- **Deferred debt:** older waves **#74–#107** not reconciled against `main` (the
  primary open cleanup); **#110** CM0 4B–4D; **#112/#113/#116** open; no fresh
  post-cleanup Repository Coherence composite on current HEAD.

---
ts: 2026-08-07T01:56:15Z
from: cn-sigma@tsc
rank: r0
class: note
---
Project vocabulary future cn-sigma must preserve

CCNF · roles α(author)/β(reviewer)/γ/δ(orchestrator)/ε · **firebreak** (α≠β) ·
**MCA** (minimum/most coherent action) / MCI · CM / CM0 (instrument-calibration) ·
artifact family `#CMSource`/`#NormalizedCMIR`/`#CompiledCM`/`#RunRequest`/
`#MeasurementReceipt`/`#CompilationReceipt`/`SandboxExecutionPlan`/
`#InstrumentSubject`/`#InstrumentAssessment` · **firewall-safe identification vs
generative correctness** · **two-sided kernel** · **profundity-generator** (the
fluent-but-empty lift the machinery must refuse) · **effect-plan**
(agent-plans / CNOS-effects) · the finalized #698 dialogue protocol · #690 memory
(r0↑ activation writes / r1↓ home compacts). CA skills: `cnos.core/skills/agent/*`.
